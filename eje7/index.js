const calcularMultiplicacion = (...numeros) => {
    const cantidadNumeros = numeros.length;
    let multiplicacion = 1;
  
    for (let i = 0; i < cantidadnumeros; i++) {
      multiplicacion *= numeros[i];
    }
  
    return `Se ingresaron ${cantidadNumeros} números y su multiplicación es ${multiplicacion}`;
  }; 
  console.log(calcularMultiplicacion());
  